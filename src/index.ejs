<%
const MY_PATH = "";
const PAGE_ID = "top";
const props = {
    path: MY_PATH
};
%><%- include(`${MY_PATH}components/_config.ejs`) %><%
const IMAGE_PATH = set_image_path(PAGE_ID);
const WEBP_PATH = set_webp_path(PAGE_ID);
%><!DOCTYPE html>
<html lang="ja">
<head>
    <%-
    include(`${MY_PATH}components/_meta.ejs`,
    {
    ...props,
    ogType: 'website'
    })
    %>
    <link rel="stylesheet" href="<%- set_file_path('top.css', `${ASSET_PATH}css/`) %>" />
    <%- include(`${MY_PATH}components/_scripts.ejs`, { ...props }) %>

</head>
<body>
<%- include(`${MY_PATH}components/_body_top.ejs`, { ...props }) %>
<div id="root" class="l-wrapper">
    <header id="header" class="l-header">
        <%- include(`${MY_PATH}components/header/_index.ejs`, { ...props }) %>
    </header>
    <!-- / header-->

    <main id="contents" class="l-contents">
    </main>
    <!-- / contents -->

    <%- include(`${MY_PATH}components/footer/_index.ejs`, { ...props }) %>
</div>
<!-- / root -->
<%- include(`${MY_PATH}components/_body_bottom.ejs`, { ...props }) %>
</body>
</html>
